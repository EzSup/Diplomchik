@using Restaurant.Client.Contracts.Bill
@using Restaurant.Client.Contracts.Enums

@if (Bill != null)
{
    <div @onclick="MoveToDetails" style="background-color: @(Bill.IsPaid ?"#242e1f":"#131313");" class="billsQuery">
        <h6>@Bill.OrderDateAndTime</h6>
        <hr />
        <div >
            <p><strong>Загальна сума:</strong> @Bill.TotalPrice ₴</p>
            @* <p style="font-size: 24px;"><strong>Дата і час замовлення:</strong> @Bill.OrderDateAndTime</p> *@
            @if(Bill.Tips > 0){
                <p><strong>Чайові:</strong> @Bill.Tips ₴</p>
            }
            <p><strong>Тип замовлення:</strong> @Localizer["orderType" + Bill.Type]</p>
            @if(Bill.Type == OrderType.InRestaurant)
            {
                <p><strong>Номер столика і час резервації:</strong> @Bill.TableNumOrDeliveryAdress</p>
            }
            else if (Bill.Type == OrderType.Delivery)
            {
                <p><strong>Адреса доставки:</strong> @Bill.TableNumOrDeliveryAdress</p>
            }

            @* <p><strong>Is Paid:</strong> @Bill.IsPaid</p> *@
        </div>        
    </div>
}


@code {
    [Parameter]
    public BillResponse Bill { get; set; }

    private void MoveToDetails()
    {
        NavManager.NavigateTo($"/billData/{Bill.Id}");
    }
}
